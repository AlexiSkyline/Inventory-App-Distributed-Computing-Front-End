export const types_provider = {
    // ! Acciones para crear una Proveedor
    addProvider: '[Provider] Add Provider',
    addProviderFailed: '[Provider] Add Provider Failed',

    // ! Acciones para las Proveedor
    getProviders: '[Providers] get Providers',
    getProvidersFailed: '[Providers] get Providers failed',

    // ! Acciones para actualizar una Proveedor
    updateProvider: '[Provider] Update Provider',
    updateProviderFailed: '[Provider] Update Provider Failed',

    // ! Acciones para eliminar una Proveedor
    deleteProvider: '[Provider] delete Provider',
    deleteProviderFailed: '[Provider] delete Provider failed',

    // ! Acciones para eliminar un mensaje
    removeMessages: '[Provider] remove messages',

    // ! Acciones activar y desactivar modo de edición
    activeModeEdit: '[Provider] active mode edit',
    desactiveModeEdit: '[Provider] desactive mode edit',

    // ! Acciones para activar y desactivar el modo de busqueda
    searchProviderActive: '[Provider] search Provider active',
    searchProviderDesactive: '[Provider] search Provider desactive',
}